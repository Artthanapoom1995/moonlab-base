# รหัสลับ — ห้ามขึ้น GitHub เด็ดขาด (repo นี้เป็น public)
config.js
# สร้างใหม่ทุกครั้งที่ build และมีทั้ง ANON_KEY, APP_PIN, SESSION_SECRET อยู่ข้างใน
worker-config.js

# ผลลัพธ์จากการ build (Cloudflare สร้างเองทุกครั้ง)
public/

# ไฟล์ที่เคยอัปโหลดตอนนำเข้าข้อมูล — เป็นข้อมูลร้าน ไม่ควรขึ้นเว็บสาธารณะ
uploads/
tmp/

# ของ wrangler ตอนทดสอบในเครื่อง
.wrangler/
*.log
.dev.vars

# ไฟล์ระบบ
.thumbnail
.image-slots.state.json
Thumbs.db
.DS_Store

# ไฟล์ส่วนตัวที่มีรหัสลับ
*.local.sql
*.local.js
setup-real.sql
