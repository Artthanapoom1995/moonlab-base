# MOONLAB Stock — คู่มือขึ้นเว็บ + แชร์ข้อมูลระหว่างคุณกับแฟน

ทั้งหมดนี้ **ฟรี** และ **ไม่ต้องลงโปรแกรมอะไรเพิ่มบนเครื่อง** (ไม่ต้องใช้ Node/Python)
ใช้เวลาครั้งแรกประมาณ 20–30 นาที ครั้งต่อไปแค่ `git push` ครั้งเดียว

| ส่วน | ใช้อะไร | ค่าใช้จ่าย |
|---|---|---|
| โค้ด | GitHub — `Artthanapoom1995/moonlab-base` | ฟรี |
| ที่เก็บเว็บ | Cloudflare Pages (มีเซิร์ฟเวอร์ในกรุงเทพ bandwidth ไม่จำกัด) | ฟรี |
| ข้อมูลกลาง | Supabase (ทำให้ไอโฟนกับไอแพดเห็นข้อมูลชุดเดียวกัน) | ฟรี |

---

## ⚠️ กฎข้อเดียวที่ห้ามพลาด

repo นี้เป็น **Public** — ใครก็อ่านโค้ดได้

**ห้ามใส่รหัสลับ Supabase ลงในไฟล์ที่ push ขึ้น GitHub เด็ดขาด**
(ลบทีหลังก็ไม่หมด เพราะ git เก็บประวัติย้อนหลังไว้ตลอด)

ระบบถูกวางไว้ให้ปลอดภัยอยู่แล้ว:

- `config.example.js` = ไฟล์ตัวอย่าง มีแต่ค่าว่าง → อยู่บน GitHub ได้
- `config.js` = ไฟล์รหัสลับจริงสำหรับทดสอบบนเครื่องคุณ → อยู่ใน `.gitignore` แล้ว **จะไม่ถูก push ไม่ว่าจะเผลอแค่ไหน**
- ตอน deploy จริง Cloudflare จะสร้าง `config.js` ขึ้นเองจาก Environment variables ที่เก็บไว้ในระบบมันเอง

---

## ขั้นที่ 1 — สร้างฐานข้อมูลกลางบน Supabase

1. เปิด https://supabase.com → **Start your project** → สมัครด้วยบัญชี GitHub ที่มีอยู่แล้วได้เลย
2. กด **New project**
   - Name: `moonlab-stock`
   - Database Password: กดปุ่มสุ่มแล้ว **เก็บไว้ที่ปลอดภัย** (ใช้ตอนกู้คืนเท่านั้น)
   - Region: **Southeast Asia (Singapore)** ← ใกล้ไทยที่สุด
3. รอสร้างเสร็จประมาณ 2 นาที

---

## ขั้นที่ 2 — ตั้งรหัสลับของแอป แล้วรัน SQL

1. เปิดไฟล์ `supabase-setup.sql` ในโฟลเดอร์นี้
2. หาบรรทัด `CHANGE-ME-TO-A-LONG-RANDOM-STRING` เปลี่ยนเป็นข้อความสุ่มยาวๆ ของคุณเอง
   เช่น `moonlab-8f3k-92xq-2026-vbn7` — **จำไว้ ต้องใช้อีกในขั้นที่ 3 และ 4**
3. Supabase → เมนูซ้าย **SQL Editor** → **New query**
4. คัดลอกเนื้อหา `supabase-setup.sql` **ทั้งไฟล์** ไปวาง → **Run**
5. ขึ้น `Success. No rows returned` = เรียบร้อย ✅

> ⚠️ ไฟล์ `supabase-setup.sql` ที่แก้แล้วจะมีรหัสลับอยู่ **อย่า commit ทับขึ้น GitHub**
> ถ้าจะแก้ไฟล์นี้เก็บไว้ ให้เซฟเป็นชื่ออื่นเช่น `setup-real.sql` (ถูก .gitignore ครอบไว้แล้วถ้าตั้งชื่อลงท้าย `.local.sql`)
> วิธีง่ายที่สุด: แก้ในหน้า SQL Editor ของ Supabase เลย ไม่ต้องเซฟลงเครื่อง

---

## ขั้นที่ 3 — (ทางเลือก) ทดสอบบนเครื่องตัวเองก่อน

1. คัดลอก `config.example.js` → ตั้งชื่อใหม่เป็น `config.js`
2. กรอก 3 ค่านี้ (เอามาจาก Supabase → **Project Settings**):

   | ช่อง | เอามาจาก |
   |---|---|
   | `url` | Data API → **Project URL** (`https://xxxxx.supabase.co`) |
   | `key` | API Keys → **anon** / **public** (ขึ้นต้นด้วย `eyJ...`) |
   | `token` | รหัสลับที่ตั้งเองในขั้นที่ 2 |

3. รัน `.\build.ps1` แล้ว `.\serve.ps1` → เปิด http://localhost:8080

ไฟล์ `config.js` นี้จะไม่ถูก push ขึ้น GitHub (อยู่ใน `.gitignore` แล้ว)

---

## ขั้นที่ 4 — เชื่อม GitHub เข้ากับ Cloudflare Pages

1. เปิด https://dash.cloudflare.com → สมัคร (ฟรี ไม่ต้องใส่บัตร)
2. เมนูซ้าย **Workers & Pages** → **Create** → แท็บ **Pages** → **Connect to Git**
3. อนุญาตให้ Cloudflare เข้าถึง GitHub → เลือก repo **`moonlab-base`** → **Begin setup**
4. ตั้งค่าตามนี้ (สำคัญมาก):

   | ช่อง | ใส่ว่า |
   |---|---|
   | Production branch | `main` |
   | Framework preset | `None` |
   | Build command | `bash build.sh` |
   | Build output directory | `public` |

5. กด **Environment variables (advanced)** → **Add variable** ทีละตัว 3 ตัว:

   | Variable name | Value | ประเภท |
   |---|---|---|
   | `SUPABASE_URL` | `https://xxxxx.supabase.co` | Text |
   | `SUPABASE_ANON_KEY` | `eyJ...` (anon key) | **Secret** (กดปุ่ม Encrypt) |
   | `APP_TOKEN` | รหัสลับจากขั้นที่ 2 | **Secret** (กดปุ่ม Encrypt) |

6. **Save and Deploy** → รอประมาณ 1 นาที
7. ได้ลิงก์ `https://moonlab-base.pages.dev` ← เว็บของคุณแล้ว 🎉

**อัปเดตครั้งต่อไป:** แก้โค้ด → `git push` → Cloudflare build ให้เองอัตโนมัติภายใน 1 นาที
ไม่ต้องลากไฟล์ ไม่ต้องกดอะไรเพิ่ม

---

## ขั้นที่ 5 — ติดตั้งบนไอโฟน/ไอแพด (สำคัญ)

ทำ **ทั้งสองเครื่อง** ทั้งของคุณและของแฟน:

1. เปิดลิงก์ `.pages.dev` ด้วย **Safari** (ต้อง Safari เท่านั้น — Chrome บน iOS ทำไม่ได้)
2. กดปุ่ม **แชร์** (สี่เหลี่ยมลูกศรขึ้น) → เลื่อนหา **เพิ่มไปยังหน้าจอโฮม**
3. ตั้งชื่อ `MOONLAB` → **เพิ่ม**

จากนี้เปิดจากไอคอนหน้าจอโฮมได้เลย เต็มจอไม่มีแถบ Safari และเปิดได้แม้เน็ตหลุด

> **ทำไมต้อง Add to Home Screen:** ถ้าเปิดผ่าน Safari เฉยๆ iOS จะลบข้อมูลที่เก็บในเครื่องทิ้งอัตโนมัติเมื่อไม่ได้เปิดเว็บเกิน 7 วัน
> (ข้อมูลจริงอยู่บนคลาวด์ไม่หาย แต่แบบโฮมสกรีนจะเร็วกว่าและใช้ตอนออฟไลน์ได้ดีกว่ามาก)

---

## ขั้นที่ 6 — ตั้งชื่อผู้ใช้แต่ละเครื่อง

เปิดเว็บ → แตะป้ายสถานะมุมขวาบน (`● ซิงก์แล้ว`) → พิมพ์ชื่อ เช่น `ART` บนเครื่องคุณ และ `MIU` บนเครื่องแฟน

ดูได้ว่าใครแก้อะไรล่าสุด — Supabase → SQL Editor:

```sql
select key, version, updated_by, updated_at from ml_state order by updated_at desc;
```

---

## ป้ายสถานะมุมขวาบนหมายถึงอะไร

| ป้าย | ความหมาย |
|---|---|
| 🟢 `● ซิงก์แล้ว · ART` | ข้อมูลตรงกันทุกเครื่องแล้ว |
| 🟡 `◌ กำลังซิงก์…` | กำลังส่ง/รับข้อมูล |
| 🔴 `● ออฟไลน์ · เก็บในเครื่อง` | เน็ตมีปัญหา — **ใช้งานต่อได้ปกติ** ระบบส่งขึ้นให้เองเมื่อเน็ตกลับมา |
| ⚪ `● ใช้ในเครื่องนี้เท่านั้น` | ยังไม่ได้ตั้ง Environment variables ใน Cloudflare (ข้อมูลยังไม่แชร์กัน) |

---

## ล้างข้อมูลตัวอย่างเพื่อเริ่มใช้ของจริง

ในเว็บมีปุ่ม **ล้างข้อมูลตัวอย่าง** อยู่แล้ว (ลบยอดขาย demo 30 วัน)

ถ้าอยากล้าง **ทุกอย่าง** เริ่มใหม่หมด ให้รันใน Supabase → SQL Editor
(เปลี่ยน `รหัสลับของคุณ` เป็น token จากขั้นที่ 2) แล้วล้าง Safari ทั้งสองเครื่องด้วย:

```sql
select ml_reset('รหัสลับของคุณ');
```

---

## ไฟล์ในโปรเจกต์นี้ทำอะไรบ้าง

| ไฟล์ | หน้าที่ |
|---|---|
| `MOONLAB Stock.dc.html` | **ตัวแอปจริง — แก้ที่ไฟล์นี้ไฟล์เดียว** |
| `cloud.js` | ระบบซิงก์ข้อมูลระหว่างเครื่อง (merge เมื่อแก้พร้อมกัน, คิวตอนออฟไลน์) |
| `support.js`, `image-slot.js` | ตัวรันของ Claude Design canvas — ไม่ต้องแก้ |
| `products.json` | รายการสินค้าตั้งต้น 229 SKU |
| `supabase-setup.sql` | SQL สร้างฐานข้อมูล + ล็อกความปลอดภัย |
| `config.example.js` | ตัวอย่างไฟล์ตั้งค่า (ไม่มีรหัสลับ) |
| `build.sh` | Cloudflare รันเองตอน deploy |
| `build.ps1` / `serve.ps1` | build + ทดสอบบนเครื่อง Windows |
| `sw.js`, `pwa.js`, `manifest.webmanifest` | ทำให้เป็นแอปบนหน้าจอโฮม + ใช้ตอนออฟไลน์ |

---

## ข้อควรรู้

- **Supabase ฟรีจะหยุดโปรเจกต์ถ้าไม่มีการใช้งานเลย 7 วันติด** — ร้านใช้ทุกวันจึงไม่มีปัญหา
  ถ้าโดนหยุดจริง เข้า dashboard กด Restore ได้ ข้อมูลไม่หาย
- **ใครมีลิงก์ `.pages.dev` ก็เปิดหน้าเว็บได้** (PIN 3 หลักกันได้แค่ระดับหนึ่ง)
  อยากล็อกจริงจัง: Cloudflare → **Zero Trust → Access** ใส่อีเมลคุณกับแฟน ฟรีถึง 50 คน คนนอกเปิดไม่ได้เลย
- **โฟลเดอร์ `uploads/` และ `tmp/` ถูกกันไม่ให้ขึ้น GitHub** (มีไฟล์ TikTok Seller และ Excel ของร้าน)
- **สำรองข้อมูล:** Supabase มี backup อัตโนมัติในแพลนฟรี และข้อมูลยังถูกสำเนาไว้ในเครื่องทั้งสองเครื่องเสมอ
